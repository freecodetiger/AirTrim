/// AirCut 核心库入口。
///
/// 当前处于文档建设阶段（pre-alpha）：`Sources/` 下只有骨架占位，
/// 真实实现从 M0 ASR spike（`docs/spikes/m0-asr-spike.md`）之后进入各子模块。
/// 子模块职责边界见各子目录 `README.md` 与 `docs/architecture/ownership-map.md`。
public enum AirCutCore {
    /// 语义化版本。0.0.x = 骨架 / 文档阶段。
    public static let version = "0.0.1"
}
